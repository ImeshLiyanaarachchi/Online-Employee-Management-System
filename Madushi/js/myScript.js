 
 function updateDocumentInfo()
 {
	const author = document.getElementById("author").value;
	const creationDate = document.getElementById("creationDate").value;
	
	//Get other update information from the Form
	//send this data to your server using an AJAX request or fetch API.
	//Update the database or store with the new information.
 }
 
 function deleteDocument()
 {
	 if (confirm('Are you sure you want to this document?))
	 {
		 //send an HTTP request to the server to delete the document.
	 }
 }
	 